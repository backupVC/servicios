DROP FUNCTION IF EXISTS conIVA;
DELIMITER //

CREATE FUNCTION conIVA (idLibro int) RETURNS DECIMAL
BEGIN
  DECLARE PVP DECIMAL;
  SELECT precido INTO PVP FROM libros WHERE id = idLibro;
  RETURN PVP * 1.21;
END; //
DELIMITER ;
